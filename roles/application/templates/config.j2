# Database settings
SQLALCHEMY_DATABASE_URI = "postgresql://{{ application.db.user }}:{{ application.db.upassword }}@{{ application.db.host }}/{{ application.db.name }}"
SQLALCHEMY_TRACK_MODIFICATIONS = False
URL_APPLICATION = 'http://{{ application.config.target }}:5001'

# Liste d'URL depuis lesquels on accepte des requetes croisées
URLS_COR = ['http://{{ application.config.target }}:{{ application.config.serverport }}']

SECRET_KEY = "{{ application.config.secret }}"

# Authentification crypting method (hash or md5)
PASS_METHOD = 'hash'

# Choose if you also want to fill MD5 passwords (lower security)
# Only useful if you have old application that use MD5 passwords
FILL_MD5_PASS = False

COOKIE_EXPIRATION = 3600
COOKIE_AUTORENEW = True

# SERVER
PORT = {{ application.config.serverport }}
DEBUG = False

ACTIVATE_API = True
ACTIVATE_APP = True